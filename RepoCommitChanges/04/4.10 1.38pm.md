added the guidelines nav bar to the app and wired the "Guides" button to open the guidelines page.

What I changed (summary)

Added a reusable NavBar
File: NavBar.jsx
Uses the header/nav markup from the guidelines page, includes a "Guides" Link that navigates to /guidelines.
Wired site routing
File: App.jsx
Now renders <NavBar /> above routes and adds a route for /guidelines that renders the guidelines app (App.tsx).
Added a small Button component referenced by the guidelines app
File: button.jsx
Minimal implementation to match existing usage in App.tsx
Verified everything builds
I ran npm run build — the project builds successfully (production build produced build assets).
Files added

NavBar.jsx — shared nav header used across pages.
button.jsx — small button used by the guidelines page.
Files updated

App.jsx — now renders NavBar and contains route for /guidelines.
How it works (behavior)

The nav bar shows on all pages because it is rendered by App.jsx.
Clicking the "Guides" link in the nav bar navigates to /guidelines and displays the page defined in App.tsx.
Home remains at / and still shows the existing Home content below the shared nav.
Build/test I ran

I installed/updated dependencies earlier to get Tailwind working and then ran:
npm run build
Build succeeded and output files were generated under build.